const fs = require("fs");
const path = require("path");

/**
 * Автоматически генерирует документацию для функций
 */
function generateAutoDocs() {
  const functionCode = fs.readFileSync("./checkWorkday.js", "utf8");

  const docs = `
# 📚 Автоматическая документация

## Функция checkWorkday()

### Описание
Проверяет, является ли текущий день рабочим или выходным.

### Возвращаемое значение
- **\`true\`** - рабочий день (понедельник-пятница)
- **\`false\`** - выходной день (суббота-воскресенье)

### Использование
\`\`\`javascript
// Пример использования
const isWorkday = checkWorkday();

if (isWorkday) {
  console.log("Время работать!");
} else {
  console.log("Можно отдыхать!");
}
\`\`\`

### Логика работы
- Понедельник (1) - Пятница (5): рабочий день
- Суббота (6), Воскресенье (0): выходной день

### Пример вывода в консоль
\`\`\`
🎉 Сегодня ВЫХОДНОЙ!
// или
💼 Сегодня РАБОЧИЙ день.
\`\`\`

---

*Документация сгенерирована автоматически*
`;

  fs.writeFileSync("./DOCUMENTATION.md", docs);
  console.log("✅ Документация сгенерирована в DOCUMENTATION.md");
}

// Запускаем если файл выполняется напрямую
if (require.main === module) {
  generateAutoDocs();
}

module.exports = generateAutoDocs;
